# Files and folders generated by Godot
#########################################

# Godot's cache and temporary data folder.
# **Important:** Some users advise NOT ignoring the .godot/uid_cache.bin file
# to prevent re-import issues, but generally, ignoring the whole folder is
# the standard approach. If you face issues, consider removing the **.godot/**
# line, or at least adding an exception for uid_cache.bin.
.godot/

# Export configuration files (may contain sensitive information).
export_presets.cfg

# Export files and binaries
*.pck
*.zip
*.apk
*.aab
*.exe
*.dll
*.so
*.dylib
*.ts

# Temporary import files
# (Godot regenerates these from the original assets).
*.import

# Files generated by the version control system or OS
###########################################################

# Engine cache files
*.mel

# Editor/OS temporary files
*~
*.bak
.DS_Store
Thumbs.db

# Android build folder (contains generated files, only keep manually modified files)
android/build/
!android/build/config.xml
!android/build/AndroidManifest.xml
